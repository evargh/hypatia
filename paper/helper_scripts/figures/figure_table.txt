prep:
in order to generate the figures you see in the thesis, you need to run the following commands:
you need to generate our constellation
you need to generate the original constellation

you need to run your tests with the following flags:
gslchannel
pointtopointlasernetdevice
gslnetdevce

logging increases the runtime significantly (i would think its a disk access/batching issue, but im not 100% sure)
run these overnight for the five algorithms presented. so five days or so

make sure to disable the tcp sack in the optimizer

dhbp had 43983901 transmits, so 4 times that messages
and      43983347 receives
351868992 communicated to neighbors

assuming two bytes for identifying the flow, approximately 0.655 gibibytes of overhead data

2.1 (orbits around earth):
Drawn in Inkscape.

2.2 (Walker Delta example)
Follow the guide in /hypatia/satviz/README.md, and run `visualize_constellation.py`. The default is currently the constellation used in the paper.

2.3 (toroidal grid graph):
Drawn in Inkscape.

2.4 (toroidal grid graph shortest paths):
Drawn in Inkscape.

2.5 (changing physical paths):
Drawn in Inkscape.

3.1 (link length CDF):
...
have the two constellations generated--add steps
...
Set `NS_LOG="TopologySatelliteNetwork=level_debug"` in your environment.
...
go to the appropriate directories and run the right constellation for 1 second
...
Run `python3 parse_lengths.py <our console> > our_linklengths.txt`
Run `python3 link_length_plot.py --constellation_linklengths our_linklengths.txt original_linklengths.txt`

4.1 (edge utilization in toroidal and barabasi)
Run `python3 generate_backbone_graphs.py --max_users_exponent 9 --num_satellites_per_orbit 22 --num_orbits 72 --percentile 0.95` to generate the data of 95th-percentile utilization
Run `python3 plot_backbone_graphs.py --max_users_exponent 9 --percentile 0.95` to plot the data of 95th-percentile utilization.
Repeat this process for with flags `--percentile 0.75` and `--percentile 0.5` for the other two plots.

big classes of figures:
all the tails:
get the isl files of the runs you want

